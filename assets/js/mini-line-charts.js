var lineChartCanvas = d3.selectAll("ticker-block__line-chart")
		.append("svg")
		.attr("height", 100)
		.attr("width", 200)
		.attr("fill", "orange");
		
var lineData = []

// Domain Scale

// line mapping

// applying line to canvas